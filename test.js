
{Menus.map((menu, index) => (
    <>
        <li key={index}>
            <span>
                <i class="">icons</i>
            </span>
            <span>{menu.title}</span>
        </li>
    </>
))}